'use strict';

/**
 * banned-keywords-video router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::banned-keywords-video.banned-keywords-video');
