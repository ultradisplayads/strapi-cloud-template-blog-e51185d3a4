'use strict';

/**
 * forum-report service
 */

const { createCoreService } = require('@strapi/strapi').factories;

// @ts-ignore
module.exports = createCoreService('api::forum-report.forum-report', ({ strapi }) => ({
  // Custom service methods can be added here
}));
