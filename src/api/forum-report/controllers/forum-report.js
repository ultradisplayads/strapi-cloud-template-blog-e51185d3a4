'use strict';

/**
 * forum-report controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

// @ts-ignore
module.exports = createCoreController('api::forum-report.forum-report', ({ strapi }) => ({
  // Custom controller methods can be added here
}));
