'use strict';

/**
 * content-safety-keyword controller
 */

const { createCoreController } = require('@strapi/strapi').factories;
//@ts-ignore
module.exports = createCoreController('api::content-safety-keywords.content-safety-keyword', ({ strapi }) => ({
  // Custom controller methods will be added here
}));
