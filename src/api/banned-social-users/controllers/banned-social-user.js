'use strict';

/**
 * banned-social-user controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::banned-social-users.banned-social-user', ({ strapi }) => ({
  // Custom controller methods will be added here
}));
