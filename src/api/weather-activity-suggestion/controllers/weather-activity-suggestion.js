'use strict';

/**
 * weather-activity-suggestion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::weather-activity-suggestion.weather-activity-suggestion');
