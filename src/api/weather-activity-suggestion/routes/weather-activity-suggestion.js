'use strict';

/**
 * weather-activity-suggestion router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::weather-activity-suggestion.weather-activity-suggestion');
