'use strict';

/**
 * forum-reaction service
 */

const { createCoreService } = require('@strapi/strapi').factories;

// @ts-ignore
module.exports = createCoreService('api::forum-reaction.forum-reaction', ({ strapi }) => ({
  // Custom service methods can be added here
}));
