'use strict';

/**
 * trusted-channels-video service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::trusted-channels-video.trusted-channels-video');
