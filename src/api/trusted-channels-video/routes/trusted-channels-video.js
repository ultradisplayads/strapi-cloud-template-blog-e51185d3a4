'use strict';

/**
 * trusted-channels-video router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::trusted-channels-video.trusted-channels-video');
