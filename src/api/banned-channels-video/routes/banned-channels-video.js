'use strict';

/**
 * banned-channels-video router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::banned-channels-video.banned-channels-video');
