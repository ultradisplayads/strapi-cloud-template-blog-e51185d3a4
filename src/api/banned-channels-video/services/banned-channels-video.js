'use strict';

/**
 * banned-channels-video service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::banned-channels-video.banned-channels-video');
